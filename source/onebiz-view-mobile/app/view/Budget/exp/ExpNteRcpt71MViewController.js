/*
 * File: app/view/Budget/exp/ExpNteRcpt71MViewController.js
 *
 * This file was generated by Sencha Architect version 4.2.4.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.6.x Modern library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.6.x Modern. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

/*
    모바일 시스템 경비노트 :: 기타영수증 현황, 결재함 상세보기 - 컨트롤러

    @author BNK시스템/계장/김정균
    @since 2019.04.22
    @version 1.0
    @see

    << 개정이력(Modification Information) >>

    수정일		  수정자                수정내용
    ----------	------------------   --------------------------
    2019.04.22	BNK시스템/계장/김정균   최초 생성
*/
Ext.define('BNK.view.Budget.exp.ExpNteRcpt71MViewController', {
    extend: 'BaseViewController',
    alias: 'controller.ExpNteRcpt71MViewController',

    config: {
        viewId: 'ExpNteRcpt71M'
    },

    /* text -> value */
    getValue: function(code, text) {

            var codeStore = Ext.StoreMgr.get('CodeStore'), items = codeStore.getData().items;
            var cbData = [];
            var isFound = false;
            var result = '';

            for (var i = 0, l = items.length; i < l; i++) {
              if (code === items[i].data.code) {
                var children = items[i].data.children;
                for (var j = 0, l2 = children.length; j < l2; j++) {
                  var item = children[j];
                  if (text == item.text) {
                    result = item.value;
                    isFound = true;
                    break;
                  }
                }
                if (isFound) {
                  break;
                }
              }
            }
            return result;

    },

    /* [회수] 버튼 */
    onClickWithdrawal: function() {
        //Ext.Msg.confirm('회수', '회수 처리하시겠습니까?' , this.withDrawal, this);

        this.withDrawal();

    },

    /* [반송] 버튼 */
    onClickSendBack: function() {
        //Ext.Msg.confirm('반송', '반송 처리하시겠습니까?' , this.sendBack, this);

        this.sendBack();

    },

    /* [회수] */
    withDrawal: function(btn, text) {
        //if(btn === 'no') return false;

        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = null,
            docNm = '기타영수증 지출결의서';

        // 결재선 [회수] 처리
        ApprovalBoxSvc.withdrawal({
            scope                 : me,
            edocCd                : 'ExpNteRcpt71',				 	 		// 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
            docNm                 : docNm, 							 		// 문서명
            bizDvcd               : 'Exp', 							 		// 업무구분코드
            bizServiceId          : 'ExpNteSvc.saveExpNteAplyEtcReceipt',   // 연관 업무서비스ID
            aprHistId             : vm.get('aprHistId'),					// 결재이력ID
            record				  : vm.get('record'),
            approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), 	// 결재선 그리드 데이터
            docData               : {								 		// 결재문서 데이터
                records       	  : vm.get('records'), 						// 결재문서 필드값
                total			  : vm.get('total')
            }
        });



    },

    /* [반송] */
    sendBack: function(btn, text) {
        if(btn === 'no') return false;

        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = null,
            docNm = '기타영수증 지출결의서';

        // 결재선 [반송] 처리
        ApprovalBoxSvc.sendBack({
            scope                 : me,
            edocCd                : 'ExpNteRcpt71',				 	 		// 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
            docNm                 : docNm, 							 		// 문서명
            bizDvcd               : 'Exp', 							 		// 업무구분코드
            bizServiceId          : 'ExpNteSvc.saveExpNteAplyEtcReceipt',   // 연관 업무서비스ID
            aprHistId             : vm.get('aprHistId'),						 	 		// 결재이력ID
            record				  : vm.get('record'),
            detail				  : null,
            approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), 	// 결재선 그리드 데이터
            docData               : {								 		// 결재문서 데이터
                records       	  : vm.get('records'), 						// 결재문서 필드값
                total			  : vm.get('total')
            }
        });



    },

    /* [결재] 버튼 */
    onClickSanction: function(btn, text) {
        //Ext.Msg.confirm('결재', '결재 처리하시겠습니까?' , this.sanction, this);

        this.sanction();

    },

    /* [결재] */
    sanction: function(btn, text) {
        //if(btn === 'no') return false;

        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = null,
            docNm = '기타영수증 지출결의서';

        ApprovalBoxSvc.sanction({
            aprHistId			: vm.get('aprHistId'), // 결재이력ID
            record				: vm.get('record'), // 동시성 제어를 하기 위해 사용
            approvalLineRecords : refs.approvalLineGrid.getJsonAllRecords(),
            scope : me
        });
    },

    onClickDraft: function() {
        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = vm.get('aprHistId');

        // 결재선 [기안] 처리
        ApprovalBoxSvc.draft({
            scope                 : me,
            edocCd                : 'ExpNteRcpt71',				 	 		// 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
            bizDvcd               : 'Exp', 							 		// 업무구분코드
            bizServiceId          : 'ExpNteSvc.saveExpNteAplyEtcReceipt',   // 연관 업무서비스ID
            aprHistId             : aprHistId,						 	 	// 결재이력ID
            approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), 	// 결재선 그리드 데이터
            docData               : {								 		// 결재문서 데이터
                afterchange  	  : vm.get('records'), 						// 결재문서 필드값
                //total			  : vm.get('total')
            }
        });
    },

    Initialize: function(component, eOpts) {
        var me = this,
            vm = me.getViewModel(),
            refs = me.getReferences(),
            aprHistId = component.initialConfig.aprHistId,
            record = component.initialConfig.record,
            command = component.command,
            docData = component.docData,
            afterchange = docData.afterchange,
            items = afterchange.items,
            aplTct = afterchange.aplTct, // 건수
            aplTotSmamt = afterchange.aplTotSmamt, // render 합계금액
            str = String(aplTotSmamt),
            aplTotSmamtRender = str.replace(/(\d)(?=(?:\d{3})+(?!\d))/g, '$1,'),
            fromDoc = component.initialConfig.fromDoc;

        if(fromDoc){
            var dd = record.aplDt;
            var aplDtRender = DateUtils.parseDate(dd); // 상신일 (기안한 날짜)
            refs.item_container.getViewModel().set('aplDt', aplDtRender);
        } else {
            refs.item_container.getViewModel().set('aplDt', record.rgdt); // 상신일
        }

        var data = {
            aplTct : aplTct,
            aplTotSmamtRender : aplTotSmamtRender
        };
        vm.set('data', data);
        vm.set('items', items); // item_container 에서도 바로 get('items') 로 사용 가능
        vm.set('docData', docData);
        vm.set('aplTct', aplTct); // 건수
        vm.set('aplTotSmamtRender', aplTotSmamtRender); // render 합계금액

        vm.set('aprHistId', aprHistId);
        vm.set('record', record);
        vm.set('records', items);

        refs.item_container.getViewModel().set('records', items);
        refs.item_container.getViewModel().set('record', record);
        refs.item_container.getViewModel().set('command', command);
        refs.item_container.getViewModel().set('aprHistId', aprHistId);

        // item 수만큼 생성
        refs.item_container.controller.onAplyAddForm();

        // 결재문서 초기화 설정
        ApprovalBoxSvc.setup({
            command   : command,
            viewModel : vm,
            aprHistId : aprHistId,
            edocCd    : 'ExpNteRcpt71',
            scope     : me
        });

    }

});