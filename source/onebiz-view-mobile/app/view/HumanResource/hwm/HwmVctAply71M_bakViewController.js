/*
 * File: app/view/HumanResource/hwm/HwmVctAply71M_bakViewController.js
 *
 * This file was generated by Sencha Architect version 4.2.4.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.6.x Modern library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.6.x Modern. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

/*
    모바일 시스템 근태관리 :: 휴가신청 결재함 상세 - 컨트롤러

    @author BNK시스템/계장/김정균
    @since 2019.07.02
    @version 1.0
    @see

    << 개정이력(Modification Information) >>

    수정일		  수정자                수정내용
    ----------	------------------   --------------------------
    2019.07.02	BNK시스템/계장/김정균   최초 생성
*/
Ext.define('BNK.view.HumanResource.hwm.HwmVctAply71M_bakViewController', {
    extend: 'BaseViewController',
    alias: 'controller.HwmVctAply71M_bakViewController',

    config: {
        viewId: 'HwmVctAply71M_bak'
    },

    /* text -> value */
    getValue: function(code, text) {

            var codeStore = Ext.StoreMgr.get('CodeStore'), items = codeStore.getData().items;
            var cbData = [];
            var isFound = false;
            var result = '';

            for (var i = 0, l = items.length; i < l; i++) {
              if (code === items[i].data.code) {
                var children = items[i].data.children;
                for (var j = 0, l2 = children.length; j < l2; j++) {
                  var item = children[j];
                  if (text == item.text) {
                    result = item.value;
                    isFound = true;
                    break;
                  }
                }
                if (isFound) {
                  break;
                }
              }
            }
            return result;

    },

    /* 회수 버튼 탭 버튼 */
    onClickWithdrawal: function() {
        //Ext.Msg.confirm('회수', '회수 처리하시겠습니까?' , this.withDrawal, this);

        this.withDrawal();

    },

    /* 반송 버튼 탭 이벤트 */
    onClickSendBack: function() {
        //Ext.Msg.confirm('반송', '반송 처리하시겠습니까?' , this.sendBack, this);

        this.sendBack();

    },

    withDrawal: function(btn, text) {
        if(btn === 'no') return false;

        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = null,
            // 변경전/후 데이터 - 업무화면에서 구현
            docNm = '휴가 신청서';

        // 결재선 [회수] 처리
        ApprovalBoxSvc.withdrawal({
            scope                 : me,
            edocCd                : 'HwmVctAply71',				 	 // 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
            docNm                 : docNm, 							 // 문서명
            bizDvcd               : 'Hwm', 							 // 업무구분코드
            bizServiceId          : 'HwmVctSvc.saveHwmVctAply', 	 // 연관 업무서비스ID
            approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), // 결재선 그리드 데이터
            aprHistId 			  : vm.get('aprHistId'),			 // 결재이력ID
            record				  : vm.get('record'), // 동시성 제어를 하기 위해 사용
            docData               : {								 // 결재문서 데이터
                afterchange : vm.get('afterchange') 			 	 // 결재문서 필드값
            }
        });


    },

    sendBack: function(btn, text) {
        if(btn === 'no') return false;

                var me               = this,
                    vm               = me.getViewModel(),
                    refs             = me.getReferences(),
                    approvalLineGrid = refs.approvalLineGrid,
                    // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
                    aprHistId        = null,
                    // 변경전/후 데이터 - 업무화면에서 구현
                    docNm = '휴가 신청서';

                // 결재선 [반송] 처리
                ApprovalBoxSvc.sendBack({
                    scope                 : me,
                    edocCd                : 'HwmVctAply71',				 	 // 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
                    docNm                 : docNm, 							 // 문서명
                    bizDvcd               : 'Hwm', 							 // 업무구분코드
                    bizServiceId          : 'HwmVctSvc.saveHwmVctAply', 	 // 연관 업무서비스ID
                    approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), // 결재선 그리드 데이터
                    aprHistId 			  : vm.get('aprHistId'),
                    record				  : vm.get('record'), // 동시성 제어를 하기 위해 사용
                    docData               : {								 // 결재문서 데이터
                        afterchange : vm.get('afterchange')
                    }
                });


    },

    /* [결재] 버튼 */
    onClickSanction: function() {
        //Ext.Msg.confirm('결재', '결재 처리하시겠습니까?' , this.sanction, this);

        this.sanction();

    },

    /* [결재] */
    sanction: function() {
        //if(btn === 'no') return false;

        	var me               = this,
        		vm               = me.getViewModel(),
        		refs             = me.getReferences(),
        		approvalLineGrid = refs.approvalLineGrid,
        		// 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
        		aprHistId        = null,
        		docNm = '휴가 신청서';

        	ApprovalBoxSvc.sanction({
        		aprHistId			: vm.get('aprHistId'), // 결재이력ID
        		record				: vm.get('record'), // 동시성 제어를 하기 위해 사용
        		approvalLineRecords : refs.approvalLineGrid.getJsonAllRecords(),
        		scope : me
        	});
    },

    onClickDraft: function() {
        // 결재선 [기안] 처리
        var me               = this,
            vm               = me.getViewModel(),
            refs             = me.getReferences(),
            approvalLineGrid = refs.approvalLineGrid,
            // 최초기안시 null, 전자문서담당자 기안시 aprHistId 설정
            aprHistId        = vm.get('aprHistId'),
            // 변경전/후 데이터 - 업무화면에서 구현
            docNm = '휴가 신청서';

        // 결재선 [기안] 처리
        ApprovalBoxSvc.draft({
            scope                 : me,
            edocCd                : 'HwmVctAply71',				 	 // 전자문서관리에서 설정된 문서코드: 화면ID를 사용함
            docNm                 : docNm, 							 // 문서명
            bizDvcd               : 'Hwm', 							 // 업무구분코드
            bizServiceId          : 'HwmVctSvc.saveHwmVctAply', 	 // 연관 업무서비스ID
            aprHistId             : aprHistId,						 	 // 결재이력ID
            approvalLineRecords   : approvalLineGrid.getJsonAllRecords(), // 결재선 그리드 데이터
            docData               : {								 // 결재문서 데이터
                fieldValues       : vm.get('fieldValues'),
                beforechange      : null,
                afterchange       : vm.get('afterchange') 			 // 결재문서 필드값
            },

        });


    },

    /* 상세화면 조회 */
    onSelect: function() {
        var me = this,
            vm = me.getViewModel(),
            refs = me.getReferences(),
            values = {
                hrWrkmVctApfId : vm.get('hrWrkmVctApfId'),
                detailSelect : true,
                overLapCheck : false
            };

        // Service 호출
        Ext.Ajax.request({
            headers : {
                viewId : this.getView().xtype, // userAlias
                serviceId : 'HwmVctSvc.findAllHwmVctDetail'
            },
            jsonData : values,
            scope : me,
            success : function(response) {
                var result = Ext.JSON.decode(response.responseText); // 서버응답결과 JSON 변환

                // Service 성공 했을 경우 기능
                console.log('휴가 상세 select Service success');

                var content = result.content;
                if(content.length > 0){
                    vm.set('detail', content);

                    var container = refs.item_container,
                        containerVm = container.getViewModel(),
                        containerController = container.controller;

                    // 휴가 일수 만큼 폼 반복 생성
                    containerController.addForm(result.content);

                } else {
                    Ext.Msg.alert('알림', '휴가 신청내역이 없습니다.');
                    return false;
                }

            },
            failure : function(response, opts){
                // Service 실패 했을 경우 기능
                console.log('휴가 상세 select Service failure');
            }
        });

    },

    Initialize: function(component, eOpts) {
        var me = this,
            vm = me.getViewModel(),
            refs = me.getReferences(),
            vctRecord = component.initialConfig.vctRecord, // HwmVctAply01M 화면에서 넘긴 데이터
            title = component.initialConfig.title;

        var aprHistId = null,
            command = null,
            docData = null,
            afterchange = null,
            hrWrkmVctApfId = null,
            record = null;

        // 휴가신청서 목록에서 상세보기
        if(vctRecord && vctRecord.beforePage === 'HwmVctAply01M'){
            aprHistId = vctRecord.comAprHistId;
            command = Constants.sanction.COMPLETED_BOX; // 단순 조회이므로 완료함 처럼 보이도록
            //docData
            afterchange = vctRecord;
            hrWrkmVctApfId = afterchange.hrWrkmVctApfId;
        }
        else { // 결재함
            aprHistId = component.initialConfig.aprHistId;
            command = component.initialConfig.command;
            docData = component.initialConfig.docData;
            afterchange = docData.afterchange;
            hrWrkmVctApfId = afterchange.hrWrkmVctApfId;
            record = component.initialConfig.record;
        }

        /* 화면에서 필요한 데이터 */
        vm.set('vct', afterchange);
        vm.set('hrWrkmVctApfId', hrWrkmVctApfId);
        /* 화면에서 필요한 데이터 */


        /* 결재에 필요한 데이터 */
        vm.set('afterchange', afterchange);
        vm.set('aprHistId', aprHistId);
        vm.set('record', record);
        vm.set('command', command);
        /* 결재에 필요한 데이터 */

        var container = refs.item_container,
            containerVm = container.getViewModel(),
            containerController = container.controller;
        containerVm.set('vct', afterchange);
        containerVm.set('aprHistId', aprHistId);
        containerVm.set('command', command);
        containerVm.set('afterchange', afterchange);
        containerVm.set('record', record);

        // 결재문서 초기화 설정
        ApprovalBoxSvc.setup({
            command   : command,
            viewModel : vm,
            aprHistId : aprHistId,
            edocCd    : 'HwmVctAply71',
            scope     : me
        });

        this.onSelect();


    }

});