/*
 * File: app/view/HumanResource/hrp/HrpOgiDept51MViewController.js
 *
 * This file was generated by Sencha Architect version 4.2.4.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.6.x Modern library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.6.x Modern. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('BNK.view.HumanResource.hrp.HrpOgiDept51MViewController', {
    extend: 'BaseViewController',
    alias: 'controller.humanresource.hrp.hrpogidept51m',

    config: {
        viewId: 'HrpOgiDept51M'
    },

    tab2ButtonSetter: function(visibleParam) {
        var me = this;
        var refs = me.getReferences();
        refs.addBtn.setVisibility(visibleParam);
        refs.modifyBtn.setVisibility(!visibleParam);
        refs.delBtn.setVisibility(!visibleParam);

        if(visibleParam){
            refs.empePsitNm.setValue("");
            refs.empeNo.setValue("");
            refs.empeNm.setValue("");
            refs.id.setValue("");
        }

    },

    getParams: function() {
        var me = this;
        var vm = me.getViewModel();
        return vm.params;

    },

    onSelect: function(pageReset) {
        var me = this,
            vm = me.getViewModel(),
            refs = me.getReferences(),
            store = me.getStore('EMPINFOStore'),
            command = vm.get('command'),
            page = arguments[0] || 1; // 페이지 초기화

        var codelist = new Array();
        codelist.push();

        var hrHrpmDeptId = refs.searchDept.getValue();
        store.getProxy().extraParams = {
            command : command,
            hrHrpmDeptId  : hrHrpmDeptId == 999 ? '': hrHrpmDeptId,
            wkingDvcdList : [Constants.wkingDvcd.WORK_NORMAL,
                             Constants.wkingDvcd.WORK_CONCURRENT,
                             Constants.wkingDvcd.WORK_DISPATCH]
        };

        if(pageReset){
            store.getProxy().extraParams.page = 1;
            var toolbar = refs.gridUp.getPlugins()[0];
            toolbar.setCurrentPage(1);
        }

        store.removeAll();
        store.loadPage(1);

    },

    onBtnShowPopupTap21: function(button, e, eOpts) {
        var me = this;
        var value =me.getParams();

        if (!value.phone) {
            Ext.Msg.alert('안내', '전화번호는 필수입력값입니다.');
            return;
        }

        location.href = 'tel:' + value.phone;
    },

    onBtnShowPopupTap1: function(button, e, eOpts) {

        var me = this;
        var value =me.getParams();

        if (!value.phone) {
            Ext.Msg.alert('안내', '핸드폰 번호는 필수입력값입니다.');
            return;
        }

        location.href = 'sms:' + value.phone;
    },

    onBtnShowPopupTap4: function(button, e, eOpts) {

        var me = this;
        var value =me.getParams();

        if (!value.compEmladr) {
            Ext.Msg.alert('안내', '이메일은 필수입력값입니다.');
            return;
        }

        location.href = 'mailto:' + value.compEmladr;
    },

    onBtnShowPopupTap11: function(button, e, eOpts) {

    },

    onBtnShowPopupTap3: function(button, e, eOpts) {
        //         var me = this,
        //             form = me.lookup('form'),
        //             values = form.getValues();

        //         if (!values.phone) {
        //             Ext.Msg.alert('안내', '전화번호는 필수입력값입니다.');
        //             return;
        //         }

        //         location.href = 'sms:' + values.phone;
    },

    onBtnCloseTap: function(button, e, eOpts) {
        this.getView().close();
    },

    initialize: function(component, eOpts) {
        var me = this,
            vm = me.getViewModel(),
            refs = me.getReferences(),
            emp = component.initialConfig;

        var phone = emp.phone; // 휴대전화번호
        if(phone && phone.length === 11){
            phone = phone.substr(0, 3) + '-' + phone.substr(3, 4) + '-' + phone.substr(7, 4);
        }
        if(phone && phone.length === 10){
            phone = phone.substr(0, 3) + '-' + phone.substr(3, 3) + '-' + phone.substr(6, 4);
        }

        if(!phone){
            phone = '-';
        }

        var neson = emp.neson; // 근무지전화번호
        if(neson && neson.length === 11){
            neson = neson.substr(0, 3) + '-' + neson.substr(3, 4) + '-' + neson.substr(7, 4);
        }
        if(neson && neson.length === 10){
            neson = neson.substr(0, 3) + '-' + neson.substr(3, 3) + '-' + neson.substr(6, 4);
        }
        if(!neson){
            neson = '-';
        }

        var fieldList = [
        'dpnm' 	  	//부서
        , 'empePsitNm'	//직위
        , 'empeRsbNm' 	//직책
        , 'jobSrNm'		//직렬
        , 'rolNm'		//사무분담
        , 'wkplcNm'		//근무지
        , 'compEmladr'	//이메일
        ];

        for(var i in fieldList){
            if(Ext.isEmpty(emp[fieldList[i]])){
                emp[fieldList[i]] = '-';
            }
        }

        vm.set('emp', emp);
        vm.set('emp.phone', phone);
        vm.set('emp.neson', neson);






        // before code
        vm.params = component.initialConfig;
        vm.set('empInfo', component.initialConfig);

    }

});